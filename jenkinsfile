pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Building..' + env.BRANCH_NAME
                deleteDir()
                checkout scm
            }
        }
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}